; Common settings for rp2350 Processor based targets
[rp2350_base]
platform =
  # TODO renovate
  https://github.com/maxgerhardt/platform-raspberrypi#cc24cfef37ed22ca9f2a6aead28c2deb76c39f24
  ; For arduino-pico >= 5.4.4
extends = arduino_base
platform_packages =
  # TODO renovate
  # Key must be "framework-arduinopico", not "arduino-pico" (PlatformIO looks up the override by
  # the exact package name platform-raspberrypi requests for board_build.core=earlephilhower; a
  # mismatched key is silently ignored and the platform falls back to its own floating git-sha
  # default instead, which is how a clean build here started pulling in iLabs_Hearth.cpp -- a
  # WiFi6-coprocessor lib that #errors on any board that doesn't define ESP_SERIAL_PORT).
  framework-arduinopico@https://github.com/earlephilhower/arduino-pico/releases/download/5.4.4/rp2040-5.4.4.zip

board_build.core = earlephilhower
board_build.filesystem_size = 0.5m
build_flags = 
  ${arduino_base.build_flags} -Wno-unused-variable -Wcast-align
  -Isrc/platform/rp2xx0
  -D__PLAT_RP2350__
  -D__FREERTOS=1
build_src_filter =
  ${arduino_base.build_src_filter} +<platform/rp2xx0/> -<nimble/> -<modules/esp32> -<mesh/eth/> -<mesh/wifi/> -<mesh/http/> -<mesh/raspihttp> -<platform/rp2xx0/pico_sleep> -<platform/rp2xx0/hardware_rosc>

lib_ignore =
  BluetoothOTA
  lvgl

lib_deps =
  ${arduino_base.lib_deps}
  ${environmental_base.lib_deps}
  ${environmental_extra.lib_deps}
  ${radiolib_base.lib_deps}
  # renovate: datasource=github-tags depName=meshtastic/Crypto packageName=meshtastic/Crypto
  https://github.com/meshtastic/Crypto/archive/591ff9a690e8168ccb7a36abde8d7783e448d395.zip
